"""Domain layer — entities, value objects and state machines.

Standard library only. No Qt, no GStreamer, no `gi`, no board/GPIO imports.
Enforced by `tests/unit/test_import_boundaries.py`, not by discipline (ADR 0001).
"""
