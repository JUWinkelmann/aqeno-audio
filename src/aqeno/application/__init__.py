"""Application layer — use cases, services and policies.

Standard library only, like `domain/`. Depends on `ports/`, never on `adapters/`
or `ui/` (ADR 0001, DEVELOPMENT.md § Rules the layout enforces).
"""
